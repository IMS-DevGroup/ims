<%= form_for(@device_group) do |f| %>
  <% if @device_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device_group.errors.count, "error") %> prohibited this device_group from being saved:</h2>

      <ul>
      <% @device_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
