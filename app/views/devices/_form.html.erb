+<%= form_for(@device) do |f| %>
  <% if @device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device.errors.count, "error") %> prohibited this device from being saved:</h2>

      <ul>
      <% @device.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ready %><br>
    <%= f.check_box :ready %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </div>
  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.number_field :owner_id %>
  </div>

    <div class="field">
      <%= f.label :stock %><br>
      <%= f.collection_select :stock_id, Stock.all, :id, :name ,include_blank: "choose stock"%>
    </div>

    <div class="field">
      <%= f.label :device_type %><br>
      <%= f.collection_select :device_type_id, DeviceType.all, :id, :name ,include_blank: "choose devicetype"%>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
